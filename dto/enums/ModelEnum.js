/**
 * ModelEnum will be used in multiple DTOs and entities to define 
 * the type of Device user is using to login into CONNECT
 */

const ModelEnum = {
    USER,
    AUTH,
    EVENT,
    MESSAGE,
    TAGS,
    NOTIFICATION
  }
  
  module.exports = ModelEnum;