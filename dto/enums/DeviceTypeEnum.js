/**
 * DeviceTypeEnum will be used in multiple DTOs and entities to define 
 * the type of Device user is using to login into CONNECT
 */

const DeviceTypeEnum = {
  ANDROID,
  WEB
}

module.exports = DeviceTypeEnum;