/**
 * UserTypeEnum is an enum that will be used to define the type of user as per their subscription 
 * For ex: Premium, Basic
 */

const UserTypeEnum = {
    BASIC,
    PREMIUM
}

module.exports = UserTypeEnum;